public interface Interface
 {

   String carName = "Bmw";

    /*
     * All methods are and ONLY abstract by default
     * All child classes must implement, not extend
     * All data types will be static & final by default - thats why they are not used?
     * 
     */
    
     public void honkSound();
     
}
